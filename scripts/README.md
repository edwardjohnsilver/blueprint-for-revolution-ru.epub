# Telegram channel to epub

1. Channel info -> "three dots" -> Export channel history
2. Run commands
```bash
npm install

node index.js "/path/to/exported/channel.html" "tmp/Blueprint for Revolution (ru).epub"`
```
